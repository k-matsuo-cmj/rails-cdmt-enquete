<h1>Teams#index</h1>
<p>Find me in app/views/teams/index.html.erb</p>

<ul>
<% @teams.each do |team| %>
  <li>
    <%= link_to "#{team.name}", team_path(team.id) %>
    <%= "#{team.manager.user_name}" %>
  </li>
<% end %>
</ul>
